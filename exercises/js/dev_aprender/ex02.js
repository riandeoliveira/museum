// Escreva uma função que usa 2 números e retorna o maior entre eles.

function max(number1, number2) {
    return number1 > number2 ? number1 : number2;
}

let maxValue = max(5, 10);

console.log(maxValue);